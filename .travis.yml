language: python

python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4

install:
  - pip install cairocffi
  - pip install nosetests
  - pip install tinycss
  - pip install cssselect
  - pip install lxml
  - pip install pillow

script: nosetests

sudo: false
